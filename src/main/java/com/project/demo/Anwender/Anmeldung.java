package com.project.demo.Anwender;

public class Anmeldung {
/*
    private boolean angemeldet = false;

    private Anmeldung(){};

    public boolean anmelden(String email, String passwort){
        if (Registrierung.istregistriert(email)  == true){
            //login mit seinen daten => openStartseite(email);
            angemeldet = true;
            return true;
        }
        else {
            //Bitte zuerst registrieren
            return false;
        }
    }

    private boolean pruefeanmeldung(String email, String passwort){
        if (pruefepasswort(email, passwort) == true && angemeldet == false){
            return true; //anmeldedaten passen und ist nicht bereits eingeloggt
        }
       else if (pruefepasswort(email, passwort)==true && angemeldet == true){
           return false; //anmeldedaten passen, account ist aber noch eingeloggt
        }
       else{return false;}
    }

    private boolean pruefepasswort(String email,String passwort){
        //DB abfrage ob passwort zu email passt
        return true;
    }

    public void ausloggen(String email, String passwort){
        angemeldet = false;
    }
*/
}
